version: 2.1

jobs:
  master:
    docker:
      - image: cimg/base:2021.03
    steps:
      - run: echo sup
      - checkout
      - persist_to_workspace:
          root: ~/
          paths:
            - "."

workflows:
  main:
    jobs:
      - master