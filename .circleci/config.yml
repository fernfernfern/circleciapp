version: 2.1


workflows:
  example-worfklow:
    jobs:
      - build-docker-image:
          context: envars
      - use-docker-image:
          requires:
            - build-docker-image
jobs:
  jobbo:
    docker:
      - image: cimg/base:latest
    steps:
      - checkout
      - run: sleep 4
      - run: pkill circle-agent
      

