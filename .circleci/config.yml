version: 2.1

orbs:
  stopthatjob: fernfernfern/stopthatjob@1.0.0

jobs:
  build:
    docker:
    - image: circleci/node:10.15
    steps:
      - run: echo weee
      - setup_remote_docker

workflows:
  workflow:
    jobs:
    - build

