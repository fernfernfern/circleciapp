version: 2.1

jobs:
  job1:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo lol

workflows:
  integration_tests:
    jobs:
      - job1:
          post-steps:
            - run: echo $BUILD_NUM