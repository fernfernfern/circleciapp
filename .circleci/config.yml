version: 2.1

orbs:
  diag: fernfernfern/test@dev:0.0.1

jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - diag/sim-cache
    - run: sleep 5


workflows:
  workflow:
    jobs:
    - build

