version: 2.1

executors:
  default:
    docker:
      - image: cimg/base:2020.01
      - image: circleci/postgres:12.2-ram
        name: unscrubbed-db
      - image: circleci/postgres:12.2-ram
        name: empty-db

jobs:
  build:
    executor: default
    steps:
      - run: echo lol

workflows:
  example:
    jobs:
      - build


