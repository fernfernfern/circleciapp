version: 2.1

orbs:
  stopthatjob: fernfernfern/stopthatjob@1.0.0

jobs:
  build:
    docker:
    - image: circleci/node:10.15
    steps:
      - run: echo weee

workflows:
  workflow:
    jobs:
    - build:
        triggers:
        - schedule:
            cron: \"*/5 * * * *\"
            filters:
                branches:
                    only:
                        - master

