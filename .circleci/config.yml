version: 2.1

jobs:
  build:
    docker: 
      - image: circleci/python
      - image: circleci/postgres
    steps:
    - run:
        command: while true; do free -m ; sleep 5; done
        background: true
    - run: sleep 200

workflows:
  my-flow:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/