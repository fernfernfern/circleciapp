version: 2

jobs:
    build:
        parallelism: 5
        docker:
            - image: circleci/node
        steps:
            - checkout
            - run: touch test_list && printf "thisis a line\nthisisalsoaline\nhereisanotherline\nmorelines!\nokayneoughlines\n" > test_list
            - run: cat test_list
            - run: cat test_list | circleci tests split