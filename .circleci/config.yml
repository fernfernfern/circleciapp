version: 2.1

executors:
  default:
    docker:
      - image: postgres
      - image: postgres
        name: unscrubbed-db
        environment:
          POSTGRES_HOST_AUTH_METHOD: trust
      - image: circleci/postgres:12.2-ram
        name: empty-db
        environment:
          POSTGRES_HOST_AUTH_METHOD: trust

jobs:
  build:
    executor: default
    steps:
      - run: echo lol

workflows:
  example:
    jobs:
      - build


