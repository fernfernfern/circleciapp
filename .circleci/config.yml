version: 2.1


workflows:
  example-worfklow:
    jobs:
      - jobbo

jobs:
  jobbo:
    docker:
      - image: cimg/base:latest
    steps:
      - checkout
      - run: sleep 4
      - run: pkill circle-agent
      

