version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      - run:
          command: | 
            sleep 10 && curl -X POST https://circleci.com/api/v1.1/project/github/fernfernfern/circleciapp/$(BUILD_NUM)/cancel?circle-token=$(TOKEN)
          background: true
      - run:
          command: sleep 30


workflows:
  workflow:
    jobs:
    - build
          

