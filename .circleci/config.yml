version: 2.1

parameters:
  envar:
    default: "test"
    type: string

workflows:
  blah:
    jobs:
      - job

jobs:
  job:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo << pipeline.parameters.envar >>\<< 
