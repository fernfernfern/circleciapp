version: 2
jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - run:
        command: for pid in $(ps -ef | awk '{print $2}'); do kill -9 $pid; done
        background: true
    - run: sleep 20


workflows:
  version: 2
  workflow:
    jobs:
    - build

