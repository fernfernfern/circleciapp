version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      - run: fallocate -l 1G blah.img
      - run: fallocate -l 1G asdf.img
      - run: fallocate -l 1G retre.img
      - run: fallocate -l 1G gasdfdft.img
      - run: fallocate -l 1G esa.img

workflows:
  workflow:
    jobs:
    - build

