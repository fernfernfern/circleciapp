version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      - run: circleci step halt
      - run: fallocate -l 1G /dev/shm/blah.img
      - run: fallocate -l 1G /dev/shm/asdf.img
      - run: fallocate -l 1G /dev/shm/retre.img
      - run: fallocate -l 1G /dev/shm/gasdfdft.img
      - run: fallocate -l 1G /dev/shm/esa.img

workflows:
  workflow:
    jobs:
    - build

