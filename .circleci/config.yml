version: 2.1

jobs:
  build:
    machine:
      image: windows-server-2019-vs2019:201908-06
    resource_class: windows.medium
    shell: powershell.exe
    steps:
      - checkout
      - run:
          environment:
            TARGET: nightly-unsecure-as-only
          name: Start Component Build
          command: . ./test.ps1