version: 2

jobs:
    build:
        parallelism: 5
        docker:
            - image: circleci/node
        steps:
            - checkout
            - run:
                command: |
                  touch test_list
                  echo thisis a line\nthisisalsoaline\nhereisanotherline\nmorelines!\nokayneoughlines\n > test_list
                  cat test_list
                  cat test_list | circleci tests split