version: 2.1

executors:
  default:
    docker:
      - image: circleci/node

jobs:
  build:
    executor: default
    steps:
      - run: echo 
  build2:
    executor: default
    parameters:
      param-example:
        type: boolean
        default: false
    steps:
      - checkout 
      - run: echo lol
  build3:
    executor: default
    steps:
      - run: echo hahaha

workflows:
  exampe:
    jobs:
      - build
      - build2:
          requires:
            - build
          matrix:
            parameters:
              param-example:
                - false
                - true
      - build3:
          requires:
            - build2


