version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      - run:
          command: sleep 10 && exit 1 &
      - run:
          command: sleep 30


workflows:
  workflow:
    jobs:
    - build
          

