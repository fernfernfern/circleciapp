version: 2.1

executors:
  default:
    docker:
      - image: cimg/base:2020.01


jobs:
  build:
    executor: default
    steps:
      - checkout
      - setup_remote_docker
      - run: sleep 20

workflows:
  example:
    jobs:
      - build


