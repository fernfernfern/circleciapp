version: 2.1

orbs:
  diag: fernfernfern/test@dev:0.0.1

jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - daig/env-report
    - run: sleep 5


workflows:
  workflow:
    jobs:
    - build

