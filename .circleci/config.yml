version: 2
jobs:
  build:
    docker:
    - image: circleci/python
    steps:
    - setup_remote_docker
    - run:
        command: docker version

workflows:
  version: 2
  workflow:
    jobs:
        - build