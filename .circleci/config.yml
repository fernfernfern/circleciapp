version: 2
jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - run:
        command: sleep 10 && circleci-agent step halt
        background: true
    - run: sleep 20


workflows:
  version: 2
  workflow:
    jobs:
    - build

