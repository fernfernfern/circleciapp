version: 2.1

orbs:
  test: fernfernfern/test@volatile

jobs:
  build:
    docker:
    - image: dr.mrpowerscripts.com
      auth:
        username: mrps
        password: $MRPS_PASS
    steps:
    - test/sim-cache
    - run: sleep 5


workflows:
  workflow:
    jobs:
    - build

