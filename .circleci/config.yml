version: 2
jobs:
  build:
    macos:
      xcode: "9.0"
    working_directory: /Users/distiller/output
    environment:
      FASTLANE_LANE: test
    shell: /bin/bash --login -o pipefail
    steps:
      - checkout
      - run: mkdir $CIRCLE_WORKING_DIRECTORY
      - run:
          name: Fastlane
          command: bundle exec fastlane $FASTLANE_LANE
      - run:
          command: cp $CIRCLE_WORKING_DIRECTORY/scan/report.junit $FL_OUTPUT_DIR/scan/results.xml
          when: always
      - store_artifacts:
          path: /Users/distiller/output
      - store_test_results:
          path: /Users/distiller/output/scan
