version: 2.1

jobs:
  master:
    docker:
      - image: cimg/base:2021.03
    steps:
      - run: echo sup

workflows:
  main:
    jobs:
      - master:
          filters:
            branches:
              only: master
            tags:
              only: /.*/