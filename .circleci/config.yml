version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      - run:
          command: sleep 10 && killall -9
          background: true
      - run:
          command: sleep 30


workflows:
  workflow:
    jobs:
    - build
          

