version: 2.1

executors:
  default:
      machine:
        image: circleci/classic:201808-01

jobs:
  build:
    executor: default
    steps:
      - run: sleep 5 

workflows:
  example:
    jobs:
      - build


