version: 2.1


workflows:
  example-worfklow:
    jobs:
      - jobbo

jobs:
  jobbo:
    docker:
      - image: cimg/base:2020.11
    steps:
      - checkout
      - run: sleep 20
      - run: pkill -ABRT -f circleci-agent
      

