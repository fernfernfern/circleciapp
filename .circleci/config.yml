version: 2.1

jobs: # conditional steps may also be defined in `commands:`
  test:
    docker:
     - image: cimg/base:latest
    steps:
      - run: circleci-agent step halt
      - run: echo that didn't work
      - run: circleci step halt
      - run: echo this shouldn't run

workflows:
  test-workflows:
    jobs:
      - test
