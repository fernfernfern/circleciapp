version: 2.1

jobs: # conditional steps may also be defined in `commands:`
  test:
    docker:
     - image: circleci/python
    steps:
      - run: echo 1
      - run: echo 2
      - run: |
          echo "export PATH=/usr/local/bin/flutter/bin:$PATH " >> $BASH_ENV
          echo "export SYSTEM_IMAGES=\"system-images;android-29;default;x86\"" >> $BASH_ENV
      - run: echo "don't fail"

workflows:
  test-workflows:
    jobs:
      - test
