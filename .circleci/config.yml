version: 2.1
jobs:
  build:
    machine: true
    steps:
      - run: echo this is a step
  deploy:
    machine: true
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh ubuntu@52.221.66.74 'cd /var/www/asksteve-new;touch circleci.txt;'

workflows:
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build # only deploy once build job has completed
          filters:
            branches:
              only: circleci-project-setup # only deploy on the master branch