version: 2.1

jobs:
  build:
    machine: true
    steps:
      - run: sleep 100000 
      - run:
          when: always
          command: echo I ran when cancelled

workflows:
  example:
    jobs:
      - build



