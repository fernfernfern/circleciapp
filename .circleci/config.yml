version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      - run: mkfile 1g /tmp/1g
      - run: mkfile 1g /tmp/1g
      - run: mkfile 1g /tmp/1g
      - run: mkfile 1g /tmp/1g
      - run: mkfile 1g /tmp/1g

workflows:
  workflow:
    jobs:
    - build

