version: 2.1

jobs:
  build:
    docker:
    - image: circleci/python
    steps:
      -
        command: sleep 10 && exit 1
        background: true
      - 
        command: sleep 30


workflows:
  workflow:
    jobs:
    - build
          

