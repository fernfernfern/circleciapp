version: 2
jobs:
  build:
    macos:
      xcode: "9.0"
    environment:
      FASTLANE_LANE: test
    shell: /bin/bash --login -o pipefail
    steps:
      - checkout
      - run:
          name: Fastlane
          command: bundle exec fastlane $FASTLANE_LANE
