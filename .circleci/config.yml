version: 2.1

jobs:
  build:
    parallelism: 5
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo $CIRCLE_NODE_INDEX
      - run: circleci tests glob src/tests/*.test.js | circleci tests split

workflows:
  what:
    jobs:
      - build
