version: 2.1

orbs:
  test: fernfernfern/test@volatile

jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - test/sim-cache
    - run: sleep 5


workflows:
  workflow:
    jobs:
    - build

