version: 2.1

executors:
  my-executor:
    machine: 
      image: ubuntu-1604:201903-01
    working_directory: /tmp

jobs:
  flow:
    executor: my-executor
    steps:
      - run: docker version

workflows:
  btd:
    jobs:
      - flow