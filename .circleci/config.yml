version: 2.1

jobs:
  build:
    parallelism: 5
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo $CIRCLE_NODE_INDEX
      - run: circleci tests glob src/tests/*.test.js | circleci tests split
      - run: npm run test (circleci tests glob src/tests/*.test.js | circleci tests split)
  build2:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: npm run test
workflows:
  what:
    jobs:
      - build
      - build2
