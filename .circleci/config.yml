version: 2.1

jobs:
  run-first-job:
    docker:
      - image: cimg/base:2021.04
    steps:
      - run: echo weeeeeeeee

workflows:
  run-tests:
    when:
      equal: [ image, << pipeline.git.tag >> ]
    jobs:
      - run-first-job:
          filters:
            tags: 
              - /image/