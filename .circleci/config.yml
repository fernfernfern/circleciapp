version: 2
jobs:
  build:
    macos:
      xcode: "10.1"
    environment:
      FASTLANE_LANE: test
    shell: /bin/bash --login -o pipefail
    steps:
      - checkout
      - run:
          name: Simulators
          command: xcrun simctl list

