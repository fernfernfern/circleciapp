version: 2.1

executors:
  default:
      machine:
        image: circleci/classic:201703-01

jobs:
  build:
    executor: default
    steps:
      - run: lsb_release -a

workflows:
  example:
    jobs:
      - build


