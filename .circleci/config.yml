version: 2.1

executors:
  default:
    docker:
      - image: circleci/node

jobs:
  build:
    executor: default
    steps:
      - run: sleep 100000 
      - run:
          when: always
          command: echo I ran when cancelled

workflows:
  example:
    jobs:
      - build



