version: 2.1

jobs: # conditional steps may also be defined in `commands:`
  test:
    docker:
      - image: cimg/python
    steps:
      - checkout
      - setup_remote_docker
      - run: docker build .


workflows:
  test-workflows:
    jobs:
      - test
