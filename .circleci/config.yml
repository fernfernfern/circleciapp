version: 2.1

jobs:
  build:
    executorType: machine
    steps:
      - run: sleep 10 
      - run:
          when: always
          command: echo I ran when cancelled

workflows:
  example:
    jobs:
      - build



